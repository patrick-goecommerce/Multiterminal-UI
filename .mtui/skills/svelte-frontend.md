# Svelte Frontend Skill

You are working in a Svelte 4 frontend project. Follow these conventions:
- UI text must be in German
- Never put variable assignments directly in $: reactive blocks
- Use $: if (condition) initFunction() pattern for reactive initialization
- Keep components focused and under 300 lines
- Use stores for shared state across components
- Prefer bind: directives over manual event handlers where appropriate
