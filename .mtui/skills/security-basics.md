# Security Basics Skill

Security principles that apply to every task:
- Never hardcode secrets, tokens, or credentials in source code
- Use environment variables or secret managers for sensitive values
- Validate and sanitize all external input (user input, API responses)
- Use parameterized queries, never string-concatenated SQL
- Apply principle of least privilege for file and network access
- Log security-relevant events but never log sensitive data
